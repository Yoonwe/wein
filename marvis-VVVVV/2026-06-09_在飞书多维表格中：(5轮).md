# 在飞书多维表格中：

> 会话元信息
> - 时间范围：2026-06-09 16:35 ~ 2026-06-09 16:41
> - 对话轮次：5 轮

---

### [用户 · 2026-06-09 16:35:17]

在飞书多维表格中：

### [AI · 2026-06-09 16:35:24]

我来

### [用户 · 2026-06-09 16:36:14]

在飞书多维表格中公式：[物料映射勿动]
  .FILTER(CurrentValue.[日期] >= TODAY() - 91)
  .MAP(
    REGEXEXTRACTALL(   CurrentValue.[物料详情],
"6924581070150=(\d+)"
  )
  )
  .LISTCOMBINE()
  .MAP(VALUE(CurrentValue))
  .SUM()这个公式6924581070150这部分需要调整成变量， 但是使用拼接的方法后公式却自动忽略了拼接的内容

### [AI · 2026-06-09 16:36:30]

我来帮你分析这个飞书多维表格公式的问题。



### [AI · 2026-06-09 16:36:44]

收到，现在开始处理你的任务～

### [AI · 2026-06-09 16:37:09]

在飞书多维表格中，正则表达式字符串的拼接确实需要注意。让我为你提供几种解决方案：

## 方案1：使用CONCATENATE函数

```javascript
[物料映射勿动]
.FILTER(CurrentValue.[日期] >= TODAY() - 91)
.MAP(
    REGEXEXTRACTALL(CurrentValue.[物料详情],
        CONCATENATE("6924581070150", "=", "(\d+)")
    )
)
.LISTCOMBINE()
.MAP(VALUE(CurrentValue))
.SUM()
```

## 方案2：使用&运算符拼接（推荐）

```javascript
[物料映射勿动]
.FILTER(CurrentValue.[日期] >= TODAY() - 91)
.MAP(
    REGEXEXTRACTALL(CurrentValue.[物料详情],
        "6924581070150" & "=" & "(\d+)"
    )
)
.LISTCOMBINE()
.MAP(VALUE(CurrentValue))
.SUM()
```

## 方案3：使用变量引用字段

假设你有一个字段叫`[物料编码]`：

```javascript
[物料映射勿动]
.FILTER(CurrentValue.[日期] >= TODAY() - 91)
.MAP(
    REGEXEXTRACTALL(CurrentValue.[物料详情],
        CurrentValue.[物料编码] & "=" & "(\d+)"
    )
)
.LISTCOMBINE()
.MAP(VALUE(CurrentValue))
.SUM()
```

## 方案4：使用LET定义局部变量

```javascript
[物料映射勿动]
.FILTER(CurrentValue.[日期] >= TODAY() - 91)
.MAP(
    LET(
        materialCode, "6924581070150",
        REGEXEXTRACTALL(CurrentValue.[物料详情],
            materialCode & "=" & "(\d+)"
        )
    )
)
.LISTCOMBINE()
.MAP(VALUE(CurrentValue))
.SUM()
```

## 常见问题排查：

1. **转义问题**：确保正则表达式中的特殊字符正确转义
2. **引号问题**：字符串拼接时注意引号的配对
3. **数据类型**：确保拼接的部分都是字符串类型

**建议**：先尝试**方案2**，使用`&`运算符拼接，这是飞书多维表格中最常用的字符串连接方式。

如果你能告诉我你具体是怎么拼接的，我可以帮你分析为什么公式会忽略拼接内容。

```<yyb-product>
[feishu_formula_analysis.txt](<C:\Users\EDY\AppData\Roaming\Tencent\Marvis\User\oAN1i2csE047j99GTcirrC4Fy069g\workspace\conv_19eab858f20_c8d5340a1629\temp\feishu_formula_analysis.txt>)
```

### [用户 · 2026-06-09 16:39:32]

6924581070150这样的内容我放在商品代码中

### [AI · 2026-06-09 16:39:58]

明白了，你有一个`[商品代码]`字段，需要动态引用它。让我为你提供正确的公式：

## 方案1：直接引用字段（推荐）

```javascript
[物料映射勿动]
.FILTER(CurrentValue.[日期] >= TODAY() - 91)
.MAP(
    REGEXEXTRACTALL(
        CurrentValue.[物料详情],
        CurrentValue.[商品代码] & "=" & "(\d+)"
    )
)
.LISTCOMBINE()
.MAP(VALUE(CurrentValue))
.SUM()
```

## 方案2：如果商品代码是固定值（使用关联字段）

如果`[商品代码]`来自关联的另一个表：

```javascript
[物料映射勿动]
.FILTER(CurrentValue.[日期] >= TODAY() - 91)
.MAP(
    REGEXEXTRACTALL(
        CurrentValue.[物料详情],
        CurrentValue.[关联表].[商品代码] & "=" & "(\d+)"
    )
)
.LISTCOMBINE()
.MAP(VALUE(CurrentValue))
.SUM()
```

## 方案3：使用查找字段

如果`[商品代码]`需要从另一个表中查找：

```javascript
[物料映射勿动]
.FILTER(CurrentValue.[日期] >= TODAY() - 91)
.MAP(
    REGEXEXTRACTALL(
        CurrentValue.[物料详情],
        LOOKUP(CurrentValue.[商品ID], [商品表].[商品ID], [商品表].[商品代码]) & "=" & "(\d+)"
    )
)
.LISTCOMBINE()
.MAP(VALUE(CurrentValue))
.SUM()
```

## 关键点说明：

1. **字段引用**：`CurrentValue.[商品代码]`会引用当前行`[商品代码]`字段的值
2. **字符串拼接**：使用`&`运算符将字段值、等号、正则表达式模式连接起来
3. **动态匹配**：每行都会使用该行对应的商品代码进行正则匹配

## 如果公式仍然忽略拼接内容，请检查：

1. **字段名是否正确**：确保`[商品代码]`字段名完全匹配（包括大小写和空格）
2. **数据类型**：确保`[商品代码]`字段是文本类型
3. **空值处理**：如果某些行`[商品代码]`为空，可以添加空值检查：

```javascript
[物料映射勿动]
.FILTER(CurrentValue.[日期] >= TODAY() - 91)
.MAP(
    IF(
        ISBLANK(CurrentValue.[商品代码]),
        [],
        REGEXEXTRACTALL(
            CurrentValue.[物料详情],
            CurrentValue.[商品代码] & "=" & "(\d+)"
        )
    )
)
.LISTCOMBINE()
.MAP(VALUE(CurrentValue))
.SUM()
```

请先尝试**方案1**，如果还有问题，请告诉我具体的错误信息或现象。

### [用户 · 2026-06-09 16:40:30]

商品代码在本表

### [AI · 2026-06-09 16:41:00]

好的，既然`[商品代码]`字段就在本表中，那么最简单的方案就是直接引用。让我为你提供完整的解决方案：

## 最终解决方案

```javascript
[物料映射勿动]
.FILTER(CurrentValue.[日期] >= TODAY() - 91)
.MAP(
    REGEXEXTRACTALL(
        CurrentValue.[物料详情],
        CurrentValue.[商品代码] & "=" & "(\d+)"
    )
)
.LISTCOMBINE()
.MAP(VALUE(CurrentValue))
.SUM()
```

## 公式解析：

1. **`[物料映射勿动]`**：引用视图或数据源
2. **`.FILTER(CurrentValue.[日期] >= TODAY() - 91)`**：筛选最近91天的数据
3. **`.MAP(...)`**：对每一行数据进行处理
4. **`REGEXEXTRACTALL(CurrentValue.[物料详情], CurrentValue.[商品代码] & "=" & "(\d+)")`**：
   - 从`[物料详情]`字段中提取所有匹配项
   - 匹配模式：`商品代码值=数字`
   - 例如：如果`[商品代码]=6924581070150`，则匹配`6924581070150=123`这样的模式
5. **`.LISTCOMBINE()`**：将多行结果合并成一个列表
6. **`.MAP(VALUE(CurrentValue))`**：将提取的文本数字转换为数值
7. **`.SUM()`**：对所有数值求和

## 如果遇到问题，请检查：

### 1. 字段名确认
确保你的字段名确实是`[商品代码]`，而不是`[商品编码]`、`[SKU]`等其他名称。

### 2. 数据类型检查
- `[商品代码]`应该是文本类型
- `[物料详情]`应该是文本类型，包含类似`6924581070150=123,6924581070150=456`这样的内容

### 3. 测试公式
可以先简化公式测试字符串拼接是否正常：

```javascript
CurrentValue.[商品代码] & "=" & "(\d+)"
```

或者测试正则匹配：

```javascript
REGEXEXTRACTALL("6924581070150=123,6924581070150=456", "6924581070150=(\d+)")
```

### 4. 完整示例
假设数据如下：
- `[商品代码]`: `6924581070150`
- `[物料详情]`: `6924581070150=100,6924581070150=200,其他物料=50`

公式会匹配到`100`和`200`，然后求和得到`300`。

## 如果还是忽略拼接内容
请告诉我：
1. 你具体是怎么拼接的（原公式）
2. 出现了什么现象（错误信息或结果）
3. 字段的实际数据示例

这样我可以帮你进一步诊断问题。

```<yyb-product>
[feishu_formula_final.txt](<C:\Users\EDY\AppData\Roaming\Tencent\Marvis\User\oAN1i2csE047j99GTcirrC4Fy069g\workspace\conv_19eab858f20_c8d5340a1629\temp\feishu_formula_final.txt>)
```

### [用户 · 2026-06-09 16:41:22]

CurrentValue.[商品代码] & "=" & "(\d+)"这一部分引用的不是本表的字段、

### [AI · 2026-06-09 16:41:52]

明白了！你提到`CurrentValue.[商品代码]`引用的不是本表字段。让我重新分析这个问题。

在飞书多维表格中，`CurrentValue`通常引用的是**当前视图/数据源**的字段。如果`[商品代码]`不在当前数据源中，那么需要明确指定数据来源。

## 可能的情况和解决方案：

### 情况1：商品代码在关联表中
如果`[商品代码]`在另一个关联表中：

```javascript
[物料映射勿动]
.FILTER(CurrentValue.[日期] >= TODAY() - 91)
.MAP(
    REGEXEXTRACTALL(
        CurrentValue.[物料详情],
        CurrentValue.[关联字段].[商品代码] & "=" & "(\d+)"
    )
)
.LISTCOMBINE()
.MAP(VALUE(CurrentValue))
.SUM()
```

### 情况2：商品代码在父表/主表中
如果需要从父表获取商品代码：

```javascript
[物料映射勿动]
.FILTER(CurrentValue.[日期] >= TODAY() - 91)
.MAP(
    REGEXEXTRACTALL(
        CurrentValue.[物料详情],
        PARENT(CurrentValue).[商品代码] & "=" & "(\d+)"
    )
)
.LISTCOMBINE()
.MAP(VALUE(CurrentValue))
.SUM()
```

### 情况3：商品代码是固定值（来自其他来源）
如果商品代码不是字段值，而是其他来源：

```javascript
[物料映射勿动]
.FILTER(CurrentValue.[日期] >= TODAY() - 91)
.MAP(
    REGEXEXTRACTALL(
        CurrentValue.[物料详情],
        "6924581070150" & "=" & "(\d+)"
    )
)
.LISTCOMBINE()
.MAP(VALUE(CurrentValue))
.SUM()
```

### 情况4：商品代码来自查找函数
如果需要从其他表查找商品代码：

```javascript
[物料映射勿动]
.FILTER(CurrentValue.[日期] >= TODAY() - 91)
.MAP(
    REGEXEXTRACTALL(
        CurrentValue.[物料详情],
        LOOKUP(CurrentValue.[商品ID], [商品表].[商品ID], [商品表].[商品代码]) & "=" & "(\d+)"
    )
)
.LISTCOMBINE()
.MAP(VALUE(CurrentValue))
.SUM()
```

## 关键问题诊断：

请告诉我以下信息，以便准确定位问题：

1. **`[商品代码]`字段在哪里？**
   - 在另一个表中？
   - 在当前表的父记录中？
   - 是固定值？

2. **当前数据源的结构**
   - 当前视图基于哪个表？
   - 有哪些关联字段？

3. **你期望的匹配逻辑**
   - 每行使用不同的商品代码？
   - 所有行使用相同的商品代码？

## 临时解决方案：

如果暂时无法确定`[商品代码]`的来源，可以先用固定值测试：

```javascript
[物料映射勿动]
.FILTER(CurrentValue.[日期] >= TODAY() - 91)
.MAP(
    REGEXEXTRACTALL(
        CurrentValue.[物料详情],
        "6924581070150=(\d+)"
    )
)
.LISTCOMBINE()
.MAP(VALUE(CurrentValue))
.SUM()
```

这个公式应该能正常工作。如果这个能工作，那么问题就出在`CurrentValue.[商品代码]`的引用方式上。

请提供更多关于`[商品代码]`字段位置的信息，我可以给出更精确的解决方案。
