# last_update2026-05-25095235,to

> 会话元信息
> - 时间范围：2026-05-25 09:55 ~ 2026-05-25 09:59
> - 对话轮次：3 轮

---

### [用户 · 2026-05-25 09:55:08]

{'last_update': '2026-05-25 09: 52: 35', 'total': 2, 'stocks': [
        {'code': 'usNVDA', 'name': '英伟达', 'price': 215.33, 'change_pct': -1.9, 'change_amount': -4.18, 'high': 221.01, 'low': 214.8, 'volume': 169275710, 'date': '2026-05-22', 'full_time': '2026-05-22 16: 00: 01'
        },
        {'code': 'usAMD', 'name': '超威半导体', 'price': 467.51, 'change_pct': 3.99, 'change_amount': 17.92, 'high': 481.41, 'low': 461.71, 'volume': 34758602, 'date': '2026-05-22', 'full_time': '2026-05-22 16: 00: 01'
        }
    ]
}这个是我飞书多维表格中的一个单元格的文本内容，我想在另一个多维表格中通过公式提取对应的内容，另一个多维表格的字段1是usNVDA这样的内容，字段2需要通过公式从原表格中对应单元格提取215.33这样的内容，也就是一句第一个字段的内容查找到对应的price的值。

### [AI · 2026-05-25 09:55:30]

我来帮你创建一个从飞书多维表格中提取股票价格的公式。

根据你的需求，需要根据股票代码（如usNVDA）从原表格的JSON格式文本中提取对应的price值。飞书多维表格支持使用公式来实现这个功能。

以下是实现这个功能的公式：

```javascript
LET(
    // 获取原表格单元格内容（假设在A列）
    jsonText, A2,
    
    // 解析JSON
    parsed, PARSE_JSON(jsonText),
    
    // 获取股票代码（假设在B列）
    stockCode, B2,
    
    // 在stocks数组中查找匹配的股票
    stockArray, GET(parsed, "stocks"),
    
    // 查找匹配的股票对象
    matchedStock, FILTER(stockArray, GET(ITEM, "code") = stockCode),
    
    // 提取price值
    IF(
        LEN(matchedStock) > 0,
        GET(INDEX(matchedStock, 1), "price"),
        "未找到"
    )
)
```

**使用说明：**

1. **在原多维表格中**：确保股票数据以JSON格式存储在一个单元格中（如你提供的示例）

2. **在新多维表格中**：
   - **字段1**：输入股票代码（如usNVDA、usAMD）
   - **字段2**：使用上述公式，需要调整以下参数：
     - `A2`：改为原表格中JSON数据所在的单元格引用
     - `B2`：改为当前行的股票代码字段引用

**公式解释：**
- `PARSE_JSON()`：将文本解析为JSON对象
- `GET()`：从对象中获取指定字段的值
- `FILTER()`：在数组中查找匹配的股票
- `INDEX()`：获取数组中的第一个匹配项
- `IF()`：处理未找到的情况

**注意事项：**
1. 确保原表格的JSON格式与你提供的示例完全一致
2. 如果股票代码可能不存在，公式会返回"未找到"
3. 如果需要提取其他字段（如change_pct、volume等），只需将公式中的"price"改为对应字段名

你需要在飞书多维表格的公式编辑器中粘贴这个公式，并根据实际情况调整单元格引用。

### [用户 · 2026-05-25 09:56:13]

我使用的是飞书多维表格，没有你说的什么单元格这种东西，也没有A2B2这样的东西

### [AI · 2026-05-25 09:57:06]

你说得对，飞书多维表格使用字段名而非单元格坐标。以下是针对飞书多维表格的可行方案。

---

## 方案：使用文本函数提取

假设你已经在目标表格中通过**关联字段**或**查找引用字段**将原表格的JSON文本引入到当前表的一个字段（假设叫「原始数据」），那么可以在目标表格中创建公式字段，公式如下：

```
VALUE(
  MID(
    [原始数据],
    FIND("price", [原始数据], FIND([股票代码], [原始数据])) + 9,
    FIND(",", [原始数据], FIND("price", [原始数据], FIND([股票代码], [原始数据]))) 
    - FIND("price", [原始数据], FIND([股票代码], [原始数据])) 
    - 9
  )
)
```

**公式逻辑：**

1. `FIND([股票代码], [原始数据])` — 在JSON文本中定位股票代码的位置
2. 从该位置往后找 `"price":`，其值起始位置 = price的p位置 + 9（跳过 `"price": ` 共9个字符）
3. 再往后找到逗号 `,`，即为价格数字的结束位置
4. `MID` 截取中间的数字文本，`VALUE` 转为数值

---

## 关键前提：跨表数据引入

飞书多维表格的两个表之间不能直接在公式中写表名引用。你需要先建立跨表数据通道，二选一：

**方式一：关联 + 查找引用（推荐）**
1. 在目标表中创建「关联」字段，关联到原数据表
2. 创建「查找引用」字段，引用原表中存储JSON的那个字段
3. 公式字段引用这个「查找引用」字段即可

**方式二：同表处理**
如果数据量不大，直接把JSON数据复制到目标表的某个字段中，公式会更简单。

---

需要我帮你进一步说明「关联字段 + 查找引用字段」的具体配置步骤吗？

### [用户 · 2026-05-25 09:58:35]

我使用的是飞书多维表格，没有你说的什么单元格这种东西，也没有A2B2这样的东西

### [AI · 2026-05-25 09:59:05]

你说得对，飞书多维表格确实使用字段名而非单元格坐标。让我重新为你设计一个适用于飞书多维表格的解决方案。

## 方案：使用飞书多维表格的公式功能

假设你有两个表：
1. **数据源表**：包含JSON格式的股票数据（字段名为"股票数据"）
2. **目标表**：包含股票代码（字段名为"股票代码"），需要提取价格

### 步骤1：在目标表中创建关联字段
1. 在目标表中添加一个"关联"类型的字段
2. 关联到数据源表
3. 设置关联条件（如通过某个共同字段）

### 步骤2：创建查找引用字段
1. 在目标表中添加"查找引用"类型的字段
2. 选择刚才创建的关联字段
3. 选择要引用的字段（数据源表中的"股票数据"字段）

### 步骤3：创建公式字段提取价格
在目标表中创建"公式"类型的字段，使用以下公式：

```javascript
// 假设：
// [股票代码] - 目标表中的股票代码字段
// [数据引用] - 上一步创建的查找引用字段（包含JSON文本）

LET(
    jsonText, [数据引用],
    stockCode, [股票代码],
    
    // 查找股票代码在JSON中的位置
    codePos, FIND(stockCode, jsonText),
    
    // 如果找到股票代码
    IF(
        codePos > 0,
        // 从该位置往后找price字段
        LET(
            pricePos, FIND("\"price\":", jsonText, codePos),
            // 如果找到price字段
            IF(
                pricePos > 0,
                LET(
                    // price值开始位置（跳过"price":和空格）
                    valueStart, pricePos + 8,
                    // 找到price值结束位置（逗号或右大括号）
                    commaPos, FIND(",", jsonText, valueStart),
                    bracePos, FIND("}", jsonText, valueStart),
                    valueEnd, IF(commaPos > 0, commaPos, bracePos),
                    // 提取价格数值
                    VALUE(MID(jsonText, valueStart, valueEnd - valueStart))
                ),
                "未找到price字段"
            )
        ),
        "股票代码不存在"
    )
)
```

### 替代方案：如果数据量不大
如果股票数据不多，可以考虑在目标表中直接使用简化的查找公式：

```javascript
// 针对特定股票代码的硬编码方案
IF(
    [股票代码] = "usNVDA",
    VALUE(MID([数据引用], FIND("\"price\":", [数据引用], FIND("usNVDA", [数据引用])) + 8, 6)),
    IF(
        [股票代码] = "usAMD",
        VALUE(MID([数据引用], FIND("\"price\":", [数据引用], FIND("usAMD", [数据引用])) + 8, 6)),
        "未配置"
    )
)
```

**关键点：**
1. 飞书多维表格使用`[字段名]`引用字段
2. 需要先通过"关联"+"查找引用"建立跨表数据连接
3. 公式中使用`FIND()`、`MID()`、`VALUE()`等文本处理函数

你需要先创建关联字段和查找引用字段，然后才能使用上述公式。需要我详细说明关联字段的创建步骤吗？
