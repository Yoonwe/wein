# python变量，这个我要求的是一个输入项，给我一个昨日日期，格式YYYY-MM-DD

> 会话元信息
> - 时间范围：2026-08-11 09:43 ~ 2026-08-11 09:48
> - 对话轮次：7 轮

---

### [用户 · 2026-08-11 09:43:59]

python变量，这个我要求的是一个输入项，给我一个昨日日期，格式YYYY-MM-DD

### [AI · 2026-08-11 09:44:03]

Hermes，直接给可运行的输入项定义：

```python
from datetime import datetime, timedelta

# 输入项：昨日日期，格式 YYYY-MM-DD（默认自动取昨日）
yesterday = (datetime.now() - timedelta(days=1)).strftime("%Y-%m-%d")
print(yesterday)  # 2026-08-10
```

如果是要手动输入、只做校验的写法：

```python
from datetime import datetime, timedelta

# 输入项：可手动指定，留空则默认昨日
date_input = input("请输入日期（YYYY-MM-DD，直接回车用昨日）: ").strip()
yesterday = date_input or (datetime.now() - timedelta(days=1)).strftime("%Y-%m-%d")
```

要哪种场景？如果是做成函数参数或 RPA 输入框的形式，告诉我具体环境，我按你的格式改。

### [用户 · 2026-08-11 09:44:19]

RPA 输入框的形式

### [AI · 2026-08-11 09:44:33]

Hermes，影刀 RPA 里"输入框"有组件和魔法指令两种落地方式：

**方式一：输入对话框组件（推荐，最贴近输入项）**

在流程中拖入「输入对话框」组件，配置如下：

| 配置项 | 值 |
|---|---|
| 标题 | 请输入昨日日期 |
| 提示 | 格式：YYYY-MM-DD，留空默认昨日 |
| 默认值（表达式） | `DateTime.Now.AddDays(-1).ToString("yyyy-MM-dd")` |
| 输出变量 | `昨日日期` |

这样打开流程会自动弹窗，输入框里已预填昨日日期，用户可直接回车或用默认值。

**方式二：魔法指令版（输入由上层组件传入）**

```python
def 获取昨日日期(用户输入: str = "") -> str:
    # 指令名称：获取昨日日期
    # 指令描述：校验并返回昨日日期（YYYY-MM-DD），输入为空时自动取昨日
    # 输入参数：用户输入 用户填写的日期字符串
    # 输出参数：返回 标准格式的昨日日期
    import datetime

    if 用户输入:
        # 有输入则校验格式
        try:
            datetime.datetime.strptime(用户输入, "%Y-%m-%d")
            return 用户输入
        except ValueError:
            raise ValueError("日期格式错误，应为YYYY-MM-DD")
    # 无输入自动取昨日
    return (datetime.datetime.now() - datetime.timedelta(days=1)).strftime("%Y-%m-%d")
```

组合用法：流程里用「输入对话框」组件接收用户输入 → 传给魔法指令「获取昨日日期」→ 返回标准日期。

需要我把这个魔法指令按你的规范生成 .py 文件到 output 目录吗？

### [用户 · 2026-08-11 09:45:20]

流程执行失败: 变量 "DateTime" 未被定义，无法使用

### [AI · 2026-08-11 09:45:32]

Hermes，这个报错是因为「输入对话框」的默认值字段是按**普通文本**处理的，不会解析 C# 表达式，`DateTime.Now` 被当成变量名去找，自然未定义。

**最稳的解法：默认值留空，昨日日期交给魔法指令自动算。**

| 组件 | 配置 |
|---|---|
| 输入对话框 | 标题：请输入昨日日期；提示：格式YYYY-MM-DD，留空默认昨日；**默认值：留空** |
| 魔法指令「获取昨日日期」 | 入参接输入对话框的输出变量 |

魔法指令（不依赖任何表达式，直接算昨日）：

```python
def 获取昨日日期(用户输入: str = "") -> str:
    # 指令名称：获取昨日日期
    # 指令描述：校验并返回昨日日期（YYYY-MM-DD），输入为空时自动取昨日
    # 输入参数：用户输入 用户填写的日期字符串
    # 输出参数：返回 标准格式的昨日日期
    import datetime

    if 用户输入:
        try:
            datetime.datetime.strptime(用户输入, "%Y-%m-%d")
            return 用户输入
        except ValueError:
            raise ValueError("日期格式错误，应为YYYY-MM-DD")
    return (datetime.datetime.now() - datetime.timedelta(days=1)).strftime("%Y-%m-%d")
```

流程逻辑：输入对话框（默认空）→ 魔法指令 → 拿到标准昨日日期。

如果你一定要在弹窗里**预填**昨日日期，就别用输入对话框的默认值，改用「获取当前时间」组件 → 输出变量 → 再用「执行Python代码」：

```python
昨日日期 = (当前时间 - datetime.timedelta(days=1)).strftime("%Y-%m-%d")
```

然后把 `昨日日期` 填到输入对话框默认值（注意此时要用**变量赋值**方式，不是表达式）。需要我生成完整 .py 魔法指令文件到 output 目录吗？

### [用户 · 2026-08-11 09:46:12]

我没有当前时间这个变量，我要求直接给我输出

### [AI · 2026-08-11 09:46:17]

Hermes，直接复制这段魔法指令即可，不依赖任何现有变量，输入框留空也能自动算昨日并输出：

```python
def 获取昨日日期(用户输入: str = "") -> str:
    # 指令名称：获取昨日日期
    # 指令描述：校验并返回昨日日期（YYYY-MM-DD），输入为空时自动取昨日
    # 输入参数：用户输入 用户填写的日期字符串
    # 输出参数：返回 标准格式的昨日日期
    import datetime

    if 用户输入:
        try:
            datetime.datetime.strptime(用户输入, "%Y-%m-%d")
            return 用户输入
        except ValueError:
            raise ValueError("日期格式错误，应为YYYY-MM-DD")
    return (datetime.datetime.now() - datetime.timedelta(days=1)).strftime("%Y-%m-%d")
```

用法：
1. 把这个指令保存为魔法指令
2. 流程里调用「获取昨日日期」，**入参留空**
3. 输出变量 `昨日日期` 直接用，值就是 `2026-08-10`（今天 08-11，自动算昨日）

整个流程不再需要"当前时间"变量，也不需要输入对话框默认值，一步出结果。

### [用户 · 2026-08-11 09:46:54]

我不要魔法指令，我要输入框，直接定义

### [用户 · 2026-08-11 09:46:58]

我不要魔法指令，我要输入框，直接定义

### [AI · 2026-08-11 09:47:06]

Hermes，按你说的，不用魔法指令，只用影刀自带输入框直接定义变量：

**操作步骤：**

1. 在流程开始处**新建变量**：`昨日日期`，类型选「字符串」，默认值留空
2. 拖入「输入对话框」组件（基础指令 → 交互 → 输入对话框）
3. 配置：

| 配置项 | 值 |
|---|---|
| 标题 | 请输入昨日日期 |
| 提示 | 格式：YYYY-MM-DD |
| 默认值 | 留空（**不要写任何表达式**） |
| 输出变量 | 选择 `昨日日期` |

4. 后续所有需要用到的地方，直接引用变量 `昨日日期` 即可

流程跑起来就是：弹窗让你输入日期 → 你填 `2026-08-10` → 变量 `昨日日期` 就存了这个值，后续直接用。

这样就没有魔法指令、没有 DateTime 表达式，不会再报变量未定义。唯一注意：日期是你**手动输入**的，不会自动算昨日。如果你要"打开弹窗时默认显示昨日日期"，那必须靠表达式或指令算，两者选一个，你定。

### [用户 · 2026-08-11 09:48:20]

直接在输入框定义不可实现吗

### [AI · 2026-08-11 09:48:29]

Hermes，可以实现，分两层说清楚：

**1. 输出变量可以直接在输入框里定义**

影刀「输入对话框」组件的**输出变量**字段，不需要你先在变量面板新建。直接在字段里输入变量名（比如 `昨日日期`），下拉会出现「新建变量」，选中即可——输入框组件自身就完成了变量定义，流程里直接引用 `昨日日期` 就行。

**2. 默认值做不到"直接写昨日"**

输入框的默认值字段是纯文本，只能填固定内容（如 `2026-08-10`），无法在里面算昨天。这是组件限制，不是配置问题。想默认显示昨日，唯一路径是：在输入框**前面**用任意一步算出昨日日期存入变量 → 输入框默认值绑定该变量。

所以你的最终配置就是：

| 配置项 | 值 |
|---|---|
| 标题 | 请输入昨日日期 |
| 提示 | 格式：YYYY-MM-DD |
| 默认值 | 留空（或填固定日期） |
| 输出变量 | 直接输入 `昨日日期`，选择新建 |

这就是"直接在输入框定义"的完整实现：变量由输入框产出，后续直接用，不涉及魔法指令、不涉及 DateTime。
